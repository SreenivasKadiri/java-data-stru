package challe;

import java.util.Arrays;

import static junit.framework.TestCase.assertTrue;
import static junit.framework.TestCase.*;
import org.junit.Test;

public class ArrayShiftTest {
    @Test public void testSomeArrayShiftMethod() {
        //ArrayShift classUnderTest = new ArrayShift();
        //String[] input = {"1","2","9","7","8","20"};
        //String [] newArray =  classUnderTest.insertShiftArray(input);
        System.out.println("New Array");
        //assertTrue("someLibraryMethod should return 'true'", classUnderTest.insertShiftArray(input) );
        assertTrue("someLibraryMethod should return 'true' ", true );
        //assertEquals(Arrays.toString(input), Arrays.toString(new String[]{"1", "2", "9", "7", "9", "20","30"}));
        //assertArrayEquals();
    }
}
